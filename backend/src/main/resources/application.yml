server:
  port: 8080

karp:
  sources-dir: ${KARP_SOURCES_DIR:/app/sources}
  wiki-dir: ${KARP_WIKI_DIR:/app/wiki}
  anthropic-api-key: ${ANTHROPIC_API_KEY}
  qdrant-host: ${QDRANT_HOST:localhost}
  qdrant-port: ${QDRANT_PORT:6334}
  top-k: 5

spring:
  ai:
    mcp:
      server:
        enabled: true
        name: karp-wiki
        version: 1.0.0
