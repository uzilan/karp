server:
  port: 7777

karp:
  qdrant-host: ${QDRANT_HOST:localhost}
  qdrant-port: ${QDRANT_PORT:6334}
  top-k: 5

spring:
  ai:
    mcp:
      server:
        enabled: true
        name: karp-wiki
        version: 1.0.0
